# deepclause-pi declares pi-ai and pi-coding-agent as peer dependencies.
# s2h only reads deepclause-pi's shipped assets in Phase 1 and will consume
# those peers explicitly when `create` (Phase 2) lands.
legacy-peer-deps=true
